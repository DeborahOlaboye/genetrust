version: '3.8'

services:
  app:
    build:
      context: .
      target: runner
    container_name: genetrust-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # Add other environment variables here
      # - DATABASE_URL=postgresql://user:password@db:5432/genetrust
    # Uncomment and configure the database service if needed
    # depends_on:
    #   - db
  
  # Database service example (PostgreSQL)
  # db:
  #   image: postgres:15-alpine
  #   container_name: genetrust-db
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_USER=genetrust
  #     - POSTGRES_PASSWORD=your_secure_password
  #     - POSTGRES_DB=genetrust
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# Volumes for persistent data
# volumes:
#   postgres_data:
